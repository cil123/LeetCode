//wrong version, will revise it later.
/**
 * Definition for binary tree
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
public class Solution {
    public void flatten(TreeNode root) {
        // Start typing your Java solution below
        // DO NOT write main() function
        if(root == null){
            return;
        }
        
        TreeNode tn = flat(root);
    }
    public TreeNode flat(TreeNode root){
        
        TreeNode left = root.left;
        TreeNode right = root.right;
        if(left == null && right == null){
            return root;
        }
            
        if(left != null){
            root.left = null;
            root.right = left;
            root = flat(left); // here i made small mistake. i used root.left but it's null..
            if(right != null){
                root.right = right;
                 root = flat(right);
            }else{
                return root;
            }
        }else{          //left == null;
            root.right = right;
            root = flat(root.right);
        }
        
        return root;
    }
}
